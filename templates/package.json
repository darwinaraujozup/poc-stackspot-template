{
  "name": "templates",
  "version": "1.0.0",
  "description": "You can use jinja to make a template-data folder more dynamic.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    {% if inputs.jest %}
    "jest": "^27.5.1"
    {% endif %}
  }
}
